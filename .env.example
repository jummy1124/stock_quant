# LINE Messaging API 推播設定範本
# 複製這個檔成 .env 並填入你的值。.env 已被 .gitignore 忽略，不會進 git。
#
#   Windows PowerShell:  Copy-Item .env.example .env
#   bash:                cp .env.example .env
#
# 取得方式:
#   LINE_CHANNEL_TOKEN -> LINE Developers Console > 你的 channel > Messaging API 分頁
#                         > Channel access token (long-lived) > Issue (那一長串)
#   LINE_USER_ID       -> 同 channel > Basic settings 分頁 > Your user ID (U 開頭)
#                         多人可用逗號分隔: U第一個,U第二個

LINE_CHANNEL_TOKEN=貼上你的_long_lived_channel_access_token
LINE_USER_ID=U貼上你的userId
